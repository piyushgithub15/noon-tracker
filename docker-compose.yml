services:
  tracker:
    build: .
    container_name: noon-price-tracker
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/prices.db
