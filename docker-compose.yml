services:
  tracker:
    build: .
    container_name: noon-price-tracker
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/prices.db
      - PORT=3000
